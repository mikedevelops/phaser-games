import 'phaser-ce';

export default class DebugState extends Phaser.State {
    public preload (
        game: Phaser.Game
    ) {}

    public create (
        game: Phaser.Game
    ) {}

    public update (
        game: Phaser.Game
    ) {}
}
